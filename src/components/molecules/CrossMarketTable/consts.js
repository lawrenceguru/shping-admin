import intl from 'react-intl-universal'

// eslint-disable-next-line import/prefer-default-export
export const adsModeOptions = [
  { label: intl.get('spend.crossMarketing.modes.document'), value: 'document' },
  { label: intl.get('spend.crossMarketing.modes.product'), value: 'product' }
]
